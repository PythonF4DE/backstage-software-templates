apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{values.app_name}}
  description: Simple API named ${{values.app_name}}
  # Example for optional annotations
  annotations:
    github.com/project-slug: PythonF4DE/${{values.app_name}}
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  owner: development
  lifecycle: experimental