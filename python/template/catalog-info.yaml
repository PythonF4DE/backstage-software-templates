apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{values.app_name}}
  description: Python application that displays basic info ${{values.app_name}}
  annotations:
    github.com/project-slug: PythonF4DE/${{values.app_name}}
spec:
  type: service
  owner: development
  lifecycle: experimental