# Documents for python-app
This application has two endpoints:
- `/api/v1/health`
- `/api/v1/details`
Here is how could expand on what each of those endpoints do.
# How to access the app?
You can access the app by accessing URL: `${{values.app_name}}-${{values.app_env}}.test.com/api/v1/health`